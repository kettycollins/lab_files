# 🧪 Architectural Security Review Report

## Контекст

Проведено архітектурне ревʼю FastAPI-шаблону з метою оцінки контрольованості системи,
можливості автоматизованих перевірок та наявності архітектурних сигналів безпеки.
Фокус ревʼю — не пошук окремих багів, а виявлення структурних рішень, які не дозволяють
будувати або застосовувати security-гейти.

---

### 🔴 Проблема №1: Відсутність типізації та OpenAPI-контракту

**Файл:** `main.py`

**Пояснення:**  
API-ендпоїнти (`/user`, `/login`) не мають type hints, `response_model` та схем валідації.
Обробка даних у `/login` здійснюється через `request.json()` без контракту.
У результаті API не формує OpenAPI-специфікацію, а отже є «невидимим» для автоматизованих
перевірок (DAST, fuzzing, schema validation).

**Розділ/патерн конспекту:**  
OpenAPI 3.0 контракт, Типізація даних (Pydantic)

**Виправлення:**  
Описати вхідні та вихідні дані через Pydantic-моделі та явно задати `response_model`
у FastAPI. Застосувати патерн **Explicit API Contract**, який створює стабільний сигнал
для CI/CD, SAST і DAST-інструментів.

---

### 🔴 Проблема №2: Відсутність логування та audit-сигналів

**Файл:** `main.py`

**Пояснення:**  
У застосунку повністю відсутнє логування дій користувача, спроб автентифікації та помилок.
Система не продукує подій, які можна агрегувати, аналізувати або використовувати для
аудиту та реагування на інциденти.

**Розділ/патерн конспекту:**  
Структуроване логування у stdout

**Виправлення:**  
Запровадити структуроване логування (JSON) у stdout з використанням
`structlog` або OpenTelemetry. Застосувати патерн **Observable System**,
який забезпечує наявність audit-сигналів незалежно від бізнес-логіки.

---

### 🔴 Проблема №3: Неконтрольоване використання секретів у Kubernetes

**Файл:** `deployment.yaml`

**Пояснення:**  
Секрети передаються в контейнер через `envFrom.secretRef` без використання Vault CSI.
Такий підхід не забезпечує централізований аудит доступу, ротацію секретів і контроль
того, хто та коли їх використовує.

**Розділ/патерн конспекту:**  
Vault CSI для зберігання секретів

**Виправлення:**  
Використати Vault CSI Secrets Store з монтуванням секретів через volume.
Застосувати патерн **Secrets as a Service**, який створює контрольовану точку доступу
та сигнал для політик і гейтів безпеки.

---

### 🔴 Проблема №4: Відсутність security-гейтів у CI/CD

**Файл:** `ci.yml`

**Пояснення:**  
CI pipeline виконує лише збірку Docker-образу та не містить перевірок безпеки:
SAST, secret scanning, SBOM або image scanning. У результаті CI не генерує жодних
сигналів про уразливості чи ризики.

**Розділ/патерн конспекту:**  
Security gates у CI/CD, Policy-as-Code

**Виправлення:**  
Додати етапи безпеки в CI (semgrep, gitleaks, trivy, SBOM).
Застосувати патерн **Security Gate in CI/CD**, де перевірки є обовʼязковою частиною
архітектури пайплайну.

---

### 🔴 Проблема №5: Надмірно відкрита мережева конфігурація

**Файл:** `main.tf`

**Пояснення:**  
Security Group дозволяє вхідний трафік з `0.0.0.0/0` на порт 443.
Таке налаштування порушує принцип мінімальних привілеїв і не дозволяє побудувати
мережеві гейти або автоматичні перевірки доступу.

**Розділ/патерн конспекту:**  
IaC як єдине джерело правди, Zero Trust Networking

**Виправлення:**  
Обмежити джерела трафіку до необхідних CIDR-блоків та застосувати IaC-сканування
(tfsec, checkov). Використати патерн **Network Segmentation**, який робить доступи
декларативними й перевіряльними.

---

### 🔴 Проблема №6: Відсутність контролю supply chain

**Файл:** `Dockerfile`

**Пояснення:**  
Контейнерний образ не має SBOM, не підписується та використовує базовий образ без pinning.
Це унеможливлює перевірку складу залежностей, виявлення CVE та контроль цілісності артефактів.

**Розділ/патерн конспекту:**  
SBOM і підпис артефактів (Supply Chain Security)

**Виправлення:**  
Генерувати SBOM під час збірки (syft), виконувати сканування та підпис образів (cosign).
Застосувати патерн **Verified Artifact Pipeline**, який гарантує прозорість та
перевірюваність ланцюжка постачання.

---

## ✅ Висновок

Виявлені проблеми мають архітектурний характер і повʼязані з відсутністю патернів,
які створюють сигнали для автоматичних перевірок. Поточна архітектура є функціональною,
але не контрольованою, що робить систему «мовчазною» для security-гейтів та політик.
